"use client";
import "@mantine/core/styles.css";
import { InputWithButton } from "./InputWithButton";


export default function Home() {
 



  return (
    <div className=" flex relative  w-full max-w-md pt-8 pb-24 mx-auto ">
      {/* {error && <div className="text-red-500 mb-4">{error.message}</div>}
      {isLoading && !completion && <div>Loading...</div>}
      {completion && <div className="whitespace-pre-wrap">{completion}</div>}
      <div className="bg-[#242424] fixed left-1/2 transform -translate-x-1/2 max-w-md w-full bottom-0 pb-4 flex items-center justify-center">
        <div className="  w-full   ">
          <InputWithButton
            input={input}
            setInput={setInput}
            handleSubmit={handleSubmit}
            handleInputChange={handleInputChange}
          />
        </div>
      </div> */}

    </div>
  );
}
